name: Build GDExtension for All Platforms

on:
  push:
    branches: [ main ]
    paths:
      - 'rust/**'
      - '.github/workflows/build-gdextension.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu
      
      - name: Build Linux x86_64 Release
        working-directory: rust
        run: cargo build --target x86_64-unknown-linux-gnu --release
      
      - name: Upload Linux Release Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-release
          path: rust/target/x86_64-unknown-linux-gnu/release/librust_accelerator_.so
          retention-days: 1

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
      
      - name: Build Windows x86_64 Release
        working-directory: rust
        run: cargo build --target x86_64-pc-windows-msvc --release
      
      - name: Upload Windows Release Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: rust/target/x86_64-pc-windows-msvc/release/rust_accelerator_.dll
          retention-days: 1

  build-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin, x86_64-apple-darwin
      
      - name: Build macOS aarch64 Release
        working-directory: rust
        run: cargo build --target aarch64-apple-darwin --release
      
      - name: Build macOS x86_64 Release
        working-directory: rust
        run: cargo build --target x86_64-apple-darwin --release
      
      - name: Combine Universal Binaries
        run: |
          mkdir -p combined/release
          lipo -create rust/target/x86_64-apple-darwin/release/librust_accelerator_.dylib rust/target/aarch64-apple-darwin/release/librust_accelerator_.dylib -output combined/release/librust_accelerator_.dylib
      
      - name: Upload macOS Release Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-release
          path: combined/release/librust_accelerator_.dylib
          retention-days: 1

  commit-binaries:
    needs: [build-linux, build-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: downloaded-artifacts
          merge-multiple: false
      
      - name: Organize Binaries
        run: |
          mkdir -p rust/target/release rust/target/debug
          
          # Move downloaded release artifacts to the release folder
          find downloaded-artifacts -type f -path "*-release/*" -exec cp {} rust/target/release/ \;
          
          # Copy those exact same release binaries into the debug folder to satisfy Godot
          cp -r rust/target/release/* rust/target/debug/
          
      - name: Commit and Push to Repository
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # Add BOTH folders so Git tracks the copied files
          git add rust/target/release/
          git add rust/target/debug/
          
          if git diff --staged --quiet; then
            echo "No changes in binaries. Skipping commit."
          else
            git commit -m "chore: auto-compile GDExtension release binaries [skip ci]"
            git push
          fi